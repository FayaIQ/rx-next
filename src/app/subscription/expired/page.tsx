import { SubscriptionExpiredClient } from "./subscription-expired-client";

export const dynamic = "force-dynamic";

export default function SubscriptionExpiredPage() {
  return <SubscriptionExpiredClient />;
}
